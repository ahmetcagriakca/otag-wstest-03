# wst03 Remote + Direct Commit

## Active Task
`math_utils.py` olustur. `modulo_sum(a, b, m)` ekle, 3 assert test ekle.
`python math_utils.py` calistir. Sonra **git add + commit + push origin main**.

## Closure
- Commit + push zorunlu
- `GOREV TAMAMLANDI`